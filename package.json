{
  "name": "devc-vscode",
  "displayName": "Dev Container File Tree",
  "description": "Browse and edit files inside a running dev container (devcontainer CLI / Docker) from VS Code running on the host.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.138.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onFileSystem:devc-vscode",
    "*"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Dev Container File Tree",
      "properties": {
        "devc-vscode.dockerPath": {
          "type": "string",
          "default": "docker",
          "description": "Docker CLI command used to talk to containers (a name on PATH or an absolute path)."
        },
        "devc-vscode.autoAttach": {
          "type": "boolean",
          "default": false,
          "description": "Automatically add a container's file tree to the workspace when a container bind-mounting an open folder starts, and remove it when that container stops."
        },
        "devc-vscode.openFolderCommand": {
          "type": "string",
          "default": "devc herdr",
          "description": "Command sent to the terminal by \"Open Folder in Container\"."
        }
      }
    },
    "commands": [
      {
        "command": "devc-vscode.showContainerFileTree",
        "title": "Show Container File Tree",
        "category": "Dev Container FS"
      },
      {
        "command": "devc-vscode.refresh",
        "title": "Refresh Container Files",
        "category": "Dev Container FS"
      },
      {
        "command": "devc-vscode.openFolderInContainer",
        "title": "Open Folder in Container",
        "category": "Dev Container FS"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "devc-vscode.openFolderInContainer",
          "when": "explorerResourceIsFolder && resourceScheme != devc-vscode"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.138.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "24.x",
    "typescript-eslint": "^8.61.1",
    "@vscode/vsce": "^4.0.0",
    "eslint": "^10.5.0",
    "typescript": "^6.0.3",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.0.0"
  },
  "overrides": {
    "diff": "^8.0.4",
    "serialize-javascript": "^7.0.6"
  }
}
